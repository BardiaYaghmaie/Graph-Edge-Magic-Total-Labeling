<div dir="rtl">

# برچسب‌گذاری جادویی کل یال در یک خانواده گراف چهاربخشی $G(m,n,k,t)$
*مدل ریاضی، فرمول‌بندی برنامه‌ریزی محدودیتی، و پیاده‌سازی مرجع*

**نویسنده:** بردیا یغمایی
**تاریخ:** ۱۴۰۴/۱۱/۲۳
**مخزن:** Graph-Edge-Magic-Total-Labeling

---

## چکیده

یک **برچسب‌گذاری جادویی کل یال** (EMTL) از یک گراف ساده متناهی $G=(V,E)$ یک
تناظر دوسویی $f:V\cup E\to \{1,2,\dots,|V|+|E|\}$ است به‌گونه‌ای که یک ثابت $\kappa$
وجود داشته باشد با

</div>

$$
f(u)+f(uv)+f(v)=\kappa \quad \text{for every edge } uv\in E.
$$

<div dir="rtl">

این سند مسئله EMTL مطالعه‌شده در این مخزن را برای یک خانواده پارامتری از
گراف‌های چهاربخشی $G(m,n,k,t)$ مشخص می‌کند، هویت‌های شمارشی پایه مرتبط برای بررسی
صحت را استخراج می‌کند، و یک فرمول‌بندی کامل برنامه‌ریزی محدودیتی که از طریق
CP-SAT گوگل OR-Tools حل می‌شود را ارائه می‌دهد. این مقاله همچنین اشیاء ریاضی را
مستقیماً به اجزای پیاده‌سازی Python پروژه نگاشت می‌دهد، شامل ساخت گراف، تولید
مدل، تأیید راه‌حل، و تجسم.

**کلمات کلیدی:** برچسب‌گذاری گراف، برچسب‌گذاری جادویی کل یال، برنامه‌ریزی
محدودیتی، CP-SAT، OR-Tools.

---

## ۱. مقدمه

برچسب‌گذاری‌های گراف، اعداد صحیح را به رأس‌ها و/یا یال‌ها با رعایت
محدودیت‌های ساختاری نسبت می‌دهند. **برچسب‌گذاری جادویی کل یال** یک
برچسب‌گذاری از نوع «تناظر دوسویی کلی + معادله موضعی» است: باید از هر عدد
صحیح دقیقاً یک بار در تمام رأس‌ها و یال‌ها استفاده شود، در حالی که به‌طور
هم‌زمان هر یال مجبور است همان مجموع القایی را داشته باشد.

از دیدگاه الگوریتمی، نمونه‌های EMTL چالش‌برانگیز هستند زیرا:

۱. فضای جستجو مجموعه‌ای از تناظرهای دوسویی روی $N=|V|+|E|$ شیء است که در
   بدترین حالت $N!$ است.
۲. شرط جادویی، برچسب‌های رأس و یال را از طریق $|E|$ معادله خطی که یک ثابت
   $\kappa$ مشترک دارند، جفت می‌کند.

بر این اساس، این مخزن از **برنامه‌ریزی محدودیتی** (CP) برای حل دقیق نمونه‌های
EMTL (در صورت امکان در محدوده زمانی) استفاده می‌کند. به‌ویژه، از **CP-SAT**
گوگل OR-Tools استفاده می‌کنیم که یادگیری تعارض‌محور به سبک SAT را با انتشار
به سبک CP روی متغیرهای صحیح ترکیب می‌کند.

پروژه به یک خانواده ساختاریافته از گراف‌های $G(m,n,k,t)$ با چهار افراز رأسی
و سه لایه یالی تخصص دارد که به اندازه کافی بزرگ است که جالب باشد و در عین
حال به اندازه کافی ساختاریافته که از آزمایش سیستماتیک پشتیبانی کند.

مشارکت‌های اصلی این مقاله پیاده‌سازی‌محور عبارتند از:

۱. مشخصه رسمی خانواده گراف $G(m,n,k,t)$ و محدودیت‌های EMTL.
۲. فرمول‌بندی دقیق CP-SAT با تفسیر انتشار و یادگیری تعارض متناسب با این
   مسئله.
۳. نگاشت مستقیم از مدل ریاضی به اجزای اجرایی Python، شامل ساخت، حل، و تأیید.

---

## ۲. تعاریف و نمادگذاری

در سراسر این سند، $G=(V,E)$ یک گراف ساده غیرجهت‌دار متناهی را نشان می‌دهد.

### ۲.۱. برچسب‌گذاری‌های کل

فرض کنید $N = |V|+|E|$. یک **برچسب‌گذاری کل** تابعی است به صورت

</div>

$$
f:V\cup E \to \{1,2,\dots,N\}.
$$

<div dir="rtl">

این تابع **دوسویی** است اگر یک‌به‌یک و پوشا باشد؛ به‌عبارت‌دیگر، مجموعه
چندتایی برچسب‌های نسبت‌داده‌شده دقیقاً $\{1,2,\dots,N\}$ است.

### ۲.۲. برچسب‌گذاری جادویی کل یال

یک برچسب‌گذاری کل دوسویی $f$ یک **برچسب‌گذاری جادویی کل یال** است اگر یک
ثابت $\kappa\in\mathbb{Z}$ وجود داشته باشد به‌گونه‌ای که

</div>

$$
\forall\, uv\in E:\quad f(u)+f(uv)+f(v)=\kappa.
$$

<div dir="rtl">

عدد صحیح $\kappa$ را **ثابت جادویی** (یا **مجموع جادویی**) می‌نامند.

---

## ۳. خانواده گراف $G(m,n,k,t)$

### ۳.۱. افرازهای رأسی

اعداد صحیح $m,n,k\ge 1$ و عدد صحیح $t$ با $0\le t\le n$ را ثابت می‌کنیم.
مجموعه رأس‌ها به چهار بخش مجزا تقسیم می‌شود:

- $A$ با $|A|=m$،
- $B$ با $|B|=n$،
- $C$ با $|C|=n$،
- $D$ با $|D|=k$.

بنابراین

</div>

$$
|V| = m + 2n + k.
$$

<div dir="rtl">

### ۳.۲. لایه‌های یالی

مجموعه یال‌ها اتحاد سه لایه دوبخشی است:

۱. **لایه دوبخشی کامل چپ** بین $A$ و $B$: تمام یال‌های $ab$ با $a\in A$ و
   $b\in B$ (یعنی $K_{m,n}$).
۲. **لایه دوبخشی $t$-منظم میانی** بین $B$ و $C$.
۳. **لایه دوبخشی کامل راست** بین $C$ و $D$: تمام یال‌های $cd$ با $c\in C$ و
   $d\in D$ (یعنی $K_{n,k}$).

بنابراین

</div>

$$
|E| = mn + nt + nk.
$$

<div dir="rtl">

### ۳.۳. ساخت سیرکولانت لایه $B$–$C$

$B=\{B_0,\dots,B_{n-1}\}$ و $C=\{C_0,\dots,C_{n-1}\}$ را نمایه‌گذاری
می‌کنیم. پروژه زیرگراف دوبخشی $t$-منظم را با قانون سیرکولانت می‌سازد:

</div>

$$
E_{BC}=\{(B_i,C_{(i+j)\bmod n}) : i\in\{0,\dots,n-1\},\, j\in\{0,\dots,t-1\}\}.
$$

<div dir="rtl">

**گزاره ۳.۱ (منظم بودن لایه میانی).**
برای $0\le t\le n$، گراف دوبخشی $(B\cup C, E_{BC})$ در هر دو طرف $t$-منظم
است.

*طرح اثبات.* هر $B_i$ دقیقاً مجاور $t$ رأس
$C_{(i+0)\bmod n},\dots,C_{(i+t-1)\bmod n}$ است. یک $C_r$ را ثابت می‌کنیم.
این رأس مجاور $B_{(r-0)\bmod n},\dots,B_{(r-(t-1))\bmod n}$ است که $t$ رأس
مجزا هستند. ∎

### ۳.۴. درجات در $G(m,n,k,t)$

درجات گراف کامل در هر افراز ثابت هستند:

- برای $a\in A$، $\deg(a)=n$ (متصل به تمام $B$).
- برای $b\in B$، $\deg(b)=m+t$ (تمام $A$ به‌علاوه $t$ همسایه در $C$).
- برای $c\in C$، $\deg(c)=k+t$ (تمام $D$ به‌علاوه $t$ همسایه در $B$).
- برای $d\in D$، $\deg(d)=n$ (متصل به تمام $C$).

این هویت‌ها در استدلال‌های مجموع مجمع مفید هستند (بخش ۴).

---

## ۴. هویت‌های جبری برای EMTL

این بخش یک هویت کلی که هر EMTL آن را برآورده می‌کند، استخراج می‌کند. این
هویت برای حل نمونه *ضروری نیست* (مدل CP همه چیز را اجرا می‌کند)، اما هنگام
بررسی راه‌حل‌ها یک پایای صحت مفید است.

فرض کنید $f$ یک EMTL از $G=(V,E)$ با ثابت جادویی $\kappa$ باشد. جمع معادلات
جادویی روی تمام یال‌ها می‌دهد:

</div>

$$
\sum_{uv\in E}\bigl(f(u)+f(uv)+f(v)\bigr) = \sum_{uv\in E}\kappa = |E|\kappa.
$$

<div dir="rtl">

سمت چپ را به مشارکت برچسب یالی و برچسب رأسی تجزیه می‌کنیم.

### ۴.۱. مجموع نقاط انتهایی به‌عنوان مجموع رأسی وزن‌دار با درجه

هر برچسب رأس $f(v)$ یک بار به ازای هر یال مجاور در مجموع
$\sum_{uv\in E}(f(u)+f(v))$ ظاهر می‌شود، بنابراین:

</div>

$$
\sum_{uv\in E}\bigl(f(u)+f(v)\bigr) = \sum_{v\in V}\deg(v)\,f(v).
$$

<div dir="rtl">

بنابراین:

</div>

$$
|E|\kappa = \sum_{e\in E} f(e) \;+\; \sum_{v\in V}\deg(v)\,f(v).
$$

<div dir="rtl">

### ۴.۲. استفاده از دوسویی برای حذف مجموع برچسب یالی

فرض کنید $N=|V|+|E|$، و فرض کنید

</div>

$$
S_N := \sum_{i=1}^{N} i = \frac{N(N+1)}{2}.
$$

<div dir="rtl">

از آنجا که $f$ یک تناظر دوسویی روی $V\cup E$ است،

</div>

$$
\sum_{e\in E} f(e) + \sum_{v\in V} f(v) = S_N,
$$

<div dir="rtl">

بنابراین $\sum_{e\in E}f(e)=S_N-\sum_{v\in V}f(v)$. با جایگزینی:

</div>

$$
|E|\kappa = S_N + \sum_{v\in V}\bigl(\deg(v)-1\bigr) f(v).
$$

<div dir="rtl">

**تفسیر.** ثابت جادویی دلخواه نیست؛ توسط برچسب‌های رأس و درجات محدود
می‌شود. برای خانواده $G(m,n,k,t)$، ضرایب $(\deg(v)-1)$ فقط چهار مقدار دارند:

- $n-1$ روی $A$ و $D$،
- $(m+t)-1$ روی $B$،
- $(k+t)-1$ روی $C$.

در کد، این هویت یک بررسی پس از حل مناسب علاوه بر تأیید هر یال است.

**نتیجه ۴.۱ (شرط بخش‌پذیری / پیوندگی).**
با ترتیب‌دهی مجدد (۴.۲) یک عبارت صریح برای ثابت جادویی به‌دست می‌آید:

</div>

$$
\kappa \;=\; \frac{S_N + \sum_{v\in V}(\deg(v)-1)f(v)}{|E|}.
$$

<div dir="rtl">

به‌ویژه، صورت کسر باید بر $|E|$ بخش‌پذیر باشد، که یک شرط پیوندگی لازم را
می‌دهد:

</div>

$$
S_N + \sum_{v\in V}(\deg(v)-1)f(v) \equiv 0 \pmod{|E|}.
$$

<div dir="rtl">

این به‌تنهایی وجود EMTL را تضمین نمی‌کند، اما هر راه‌حل معتبر باید آن را
برآورده کند.

### ۴.۳. کرانه‌های جهانی روی $\kappa$

چون سه برچسب *مجزا* روی هر یال جمع می‌شوند، یک کران پایین جهانی امن عبارت
است از:

</div>

$$
\kappa \ge 1+2+3=6.
$$

<div dir="rtl">

به‌طور مشابه، حداکثر مجموع ممکن سه برچسب مجزا از $\{1,\dots,N\}$ برابر
$N+(N-1)+(N-2)=3N-3$ است، بنابراین

</div>

$$
\kappa \le 3N-3.
$$

<div dir="rtl">

پیاده‌سازی از این کرانه‌ها به‌عنوان دامنه متغیر ثابت جادویی استفاده می‌کند.

---

## ۵. فرمول‌بندی برنامه‌ریزی محدودیتی (CP-SAT)

### ۵.۱. متغیرهای تصمیم

فرض کنید $N=|V|+|E|$.

- برای هر رأس $v\in V$، یک متغیر صحیح $x_v\in\{1,\dots,N\}$ معرفی می‌کنیم.
- برای هر یال $e\in E$، یک متغیر صحیح $x_e\in\{1,\dots,N\}$ معرفی می‌کنیم.
- متغیر ثابت جادویی $\kappa\in\{6,\dots,3N-3\}$ را معرفی می‌کنیم.

### ۵.۲. محدودیت‌ها

**(C1) تناظر دوسویی (AllDifferent).**
اجرا می‌کنیم که تمام برچسب‌ها متمایز باشند:

</div>

$$
\mathrm{AllDifferent}\bigl(\{x_v : v\in V\}\cup\{x_e : e\in E\}\bigr).
$$

<div dir="rtl">

از آنجا که دامنه‌ها همگی $\{1,\dots,N\}$ هستند و تعداد متغیرها $N$ است، این
امر برچسب‌ها را به یک تناظر دوسویی روی $\{1,\dots,N\}$ تبدیل می‌کند.

**(C2) محدودیت‌های مجموع جادویی.**
برای هر یال $uv\in E$:

</div>

$$
x_u + x_{uv} + x_v = \kappa.
$$

<div dir="rtl">

هیچ هدف بهینه‌سازی وجود ندارد؛ مدل کاملاً یک مسئله امکان‌سنجی است.

### ۵.۳. اندازه مدل برای $G(m,n,k,t)$

برای خانواده بخش ۳:

</div>

$$
|V| = m+2n+k,\qquad |E|=mn+nt+nk,\qquad N=|V|+|E|.
$$

<div dir="rtl">

- متغیرهای صحیح: $N$ متغیر برچسب به‌علاوه $1$ متغیر ثابت جادویی.
- محدودیت‌ها: $1$ محدودیت AllDifferent به‌علاوه $|E|$ معادله خطی.

حتی برای پارامترهای متوسط، $N$ به دلیل لایه‌های دوبخشی کامل به‌صورت درجه
دوم در $n$ رشد می‌کند، بنابراین حل عملی به‌شدت به انتشار، تعارض‌های
آموخته‌شده، و جستجوی مؤثر وابسته است.

### ۵.۴. چرا CP-SAT اینجا به‌خوبی کار می‌کند

محدودیت‌ها اتصال قوی ایجاد می‌کنند:

- هر معادله جادویی، ترکیب‌های مجاز سه برچسب مجاور و $\kappa$ مشترک را کاهش
  می‌دهد.
- AllDifferent ناسازگاری کلی را به‌سرعت منتشر می‌کند وقتی یک برچسب‌گذاری
  جزئی شروع به «مصرف» تعداد زیادی عدد می‌کند.
- CP-SAT *nogood*هایی (بندهای تعارض) یاد می‌گیرد تا از بازدید مجدد
  تخصیص‌های جزئی غیرممکن جلوگیری کند.

### ۵.۵. CP-SAT چیست

در سطح ریاضی، CP-SAT یک روش حل **مسئله ارضای محدودیت (CSP)** است:

- متغیرهای $X_1,\dots,X_r$،
- هر متغیر **دامنه** متناهی $D(X_i)\subset\mathbb{Z}$ دارد،
- محدودیت‌ها، چندتاهای مجاز از مقادیر را محدود می‌کنند.

برای EMTL متغیرهای $\{x_v\}_{v\in V}$، $\{x_e\}_{e\in E}$، و $\kappa$ را
داریم، با دامنه‌های اولیه:

</div>

$$
D(x_v)=D(x_e)=\{1,\dots,N\}, \qquad D(\kappa)=\{6,\dots,3N-3\}.
$$

<div dir="rtl">

CP-SAT این دامنه‌ها را نگه می‌دارد (و به‌طور مکرر تنگ می‌کند). وقتی
حل‌کننده یک **تصمیم** می‌گیرد (مثلاً "$x_{A_0}=11$")، این معادل افزودن یک
محدودیت اضافه و سپس اجرای مجدد همان منطق تنگ‌سازی دامنه است.

وقتی CP-SAT تشخیص می‌دهد که مجموعه‌ای از تصمیمات یک تناقض را مجبور می‌کند،
نه تنها عقب‌نشینی می‌کند؛ بلکه سعی می‌کند یک *محدودیت جدید* استخراج کند
(«این ترکیب خاص هرگز نمی‌تواند اتفاق بیفتد»)، بنابراین وقت را به رسیدن به
همان بن‌بست تلف نمی‌کند. این ایده اصلی پشت **یادگیری مبتنی بر تعارض** است.

### ۵.۶. انتشار روی محدودیت‌های جادویی (فیلترینگ دامنه)

یک محدودیت یال منفرد را برای $uv\in E$ در نظر می‌گیریم:

</div>

$$
x_u + x_{uv} + x_v = \kappa.
$$

<div dir="rtl">

فرض کنید در یک نقطه حل‌کننده فقط *کرانه‌ها* (بازه‌ها) برای هر متغیر
می‌داند:

</div>

$$
x_u\in[\ell_u, r_u],\quad x_v\in[\ell_v, r_v],\quad x_{uv}\in[\ell_e, r_e],\quad
\kappa\in[\ell_\kappa, r_\kappa].
$$

<div dir="rtl">

آنگاه تساوی کرانه‌های لازم زیر را ایجاب می‌کند (هر کدام فقط ترتیب‌دهی مجدد
تساوی و استفاده از مقادیر بدترین حالت است):

**کرانه‌های برچسب یال از نقاط انتهایی و $\kappa$:**

</div>

$$
x_{uv} = \kappa - x_u - x_v
\;\Rightarrow\;
\begin{cases}
x_{uv}\ge \ell_\kappa - r_u - r_v,\\
x_{uv}\le r_\kappa - \ell_u - \ell_v.
\end{cases}
$$

<div dir="rtl">

**کرانه‌های یک نقطه انتهایی از نقطه انتهایی دیگر، برچسب یال، و $\kappa$:**

</div>

$$
x_u = \kappa - x_{uv} - x_v
\;\Rightarrow\;
\begin{cases}
x_u\ge \ell_\kappa - r_e - r_v,\\
x_u\le r_\kappa - \ell_e - \ell_v,
\end{cases}
\qquad
x_v \text{ similarly.}
$$

<div dir="rtl">

**کرانه‌های ثابت جادویی از سه برچسب:**

</div>

$$
\kappa = x_u + x_{uv} + x_v
\;\Rightarrow\;
\begin{cases}
\kappa \ge \ell_u + \ell_e + \ell_v,\\
\kappa \le r_u + r_e + r_v.
\end{cases}
$$

<div dir="rtl">

در اصطلاح CP، اعمال این استنتاج‌ها برای کوچک کردن بازه‌ها **انتشار** (یا
**فیلترینگ دامنه**) نامیده می‌شود. این روش *صحیح* است: هرگز مقداری را که
می‌تواند در راه‌حل واقعی ظاهر شود حذف نمی‌کند. همچنین اغلب قدرتمند است:
وقتی هر دو تا از سه برچسب روی یک یال ثابت شوند، سومی ثابت می‌شود.

برای EMTL این اهمیت دارد چون هر محدودیت یالی فرم «تابعی»
$x_{uv}=\kappa-x_u-x_v$ دارد. بنابراین، همچنان که حل‌کننده برچسب‌های رأس را
ثابت می‌کند، بسیاری از متغیرهای برچسب یالی مجبور می‌شوند (یا کرانه‌های
بسیار تنگ پیدا می‌کنند)، که سپس با AllDifferent تعامل پیدا می‌کند.

### ۵.۷. انتشار از AllDifferent

محدودیت

</div>

$$
\mathrm{AllDifferent}(X_1,\dots,X_N)
$$

<div dir="rtl">

به معنای این است که هیچ دو متغیری نمی‌توانند مقدار یکسان داشته باشند.

ساده‌ترین قانون انتشار این است:

- اگر برخی $X_i$ به مقدار $a$ ثابت شود، آنگاه $a$ را از هر دامنه دیگری
  $D(X_j)$ برای $j\ne i$ حذف می‌کنیم.

اما AllDifferent می‌تواند استنتاج‌های **مبتنی بر مجموعه** نیز انجام دهد. یک
ایده استاندارد *مجموعه هال* است:

> اگر مجموعه‌ای از $h$ متغیر فقط بتواند مقادیری از مجموعه‌ای از $h$ عدد
> بگیرد، آنگاه آن $h$ عدد برای آن متغیرها «رزرو» شده و می‌توان آن‌ها را از
> دامنه تمام متغیرهای دیگر حذف کرد.

مثال: اگر دو متغیر دامنه‌های $\{1,2\}$ و $\{1,2\}$ داشته باشند، آنگاه باید
به‌نحوی از $\{1,2\}$ استفاده کنند، بنابراین هیچ متغیر دیگری نمی‌تواند از $1$
یا $2$ استفاده کند.

برای EMTL، AllDifferent *هر* برچسب رأس و *هر* برچسب یال را جفت می‌کند، به
همین دلیل است که یک مقدار برچسب یالی مجبورشده می‌تواند آبشاری از حذف‌های
دامنه را در جاهای دیگر مدل ایجاد کند.

### ۵.۸. جستجو: تصمیمات، عقب‌نشینی، و «اثبات ناشدنی بودن»

انتشار به‌تنهایی معمولاً مسئله را تمام نمی‌کند؛ ممکن است انتخاب‌های زیادی
باقی بمانند. CP-SAT سپس یک جستجوی سیستماتیک (به‌صورت مفهومی یک درخت
عقب‌نشینی) انجام می‌دهد:

۱. تمام محدودیت‌ها را **منتشر** کنید تا دیگر هیچ کاهش دامنه‌ای ممکن نباشد.
۲. اگر دامنه‌ای خالی شود، این شاخه غیرممکن است (**تعارض**).
۳. اگر همه متغیرها ثابت شوند، یک راه‌حل پیدا شده است.
۴. در غیر این صورت، یک تصمیم متغیر/مقدار انتخاب کنید و بازگشت کنید.

به‌صورت ریاضی، یک «شاخه» اضافه کردن یک محدودیت انحصاری مانند

</div>

$$
x = a \quad\text{or}\quad x \ne a,
$$

<div dir="rtl">

یا (برای دامنه‌های بازه‌ای) یک تقسیم مانند

</div>

$$
x \le c \quad\text{or}\quad x \ge c+1
$$

<div dir="rtl">

است.

اگر CP-SAT کل درخت جستجو را بررسی کند و هر شاخه به تعارض منجر شود، آنگاه
یک *اثبات* تولید کرده که CSP ناشدنی است. این با وضعیت `INFEASIBLE` در
پیاده‌سازی مطابقت دارد.

### ۵.۹. یادگیری مبتنی بر تعارض (چرا CP-SAT از تکرار اشتباهات جلوگیری می‌کند)

یک تفاوت کلیدی بین CP-SAT مدرن و عقب‌نشینی ساده **یادگیری** است: وقتی یک
تعارض رخ می‌دهد، CP-SAT تحلیل می‌کند *کدام تصمیمات آن را ایجاد کردند* و یک
محدودیت جدید ثبت می‌کند که آن ترکیب را در آینده منع می‌کند. این محدودیت
آموخته‌شده اغلب **nogood** نامیده می‌شود.

برای EMTL، تعارض‌ها معمولاً اینگونه ایجاد می‌شوند:

- یک محدودیت یالی برچسب یال $x_{uv}$ را مجبور می‌کند مقدار $a$ را بگیرد،
  اما AllDifferent قبلاً $a$ را مجبور کرده که جای دیگری استفاده شود؛ یا
- $x_{uv}$ مجبورشده خارج از $\{1,\dots,N\}$ می‌افتد (دامنه خالی می‌شود)؛ یا
- مجبور کردن بسیاری از یال‌ها از طریق همان $\kappa$ هیچ راه سازگاری برای
  مجزا نگه‌داشتن همه برچسب‌ها باقی نمی‌گذارد.

یادگیری قدرتمند است چون EMTL زیرساخت‌های مشابه زیادی دارد (بسیاری از یال‌ها
با همان فرم)، بنابراین «الگوهای بد» در بخش‌های مختلف جستجو تکرار می‌شوند.
یک nogood آموخته‌شده مانع از کشف مجدد الگوی مشابه توسط CP-SAT تحت ترتیب
اکتشاف متفاوت می‌شود.

### ۵.۱۰. دیدگاه معادل «رأس-اول»

از معادلات جادویی،

</div>

$$
x_{uv} = \kappa - x_u - x_v.
$$

<div dir="rtl">

بنابراین، وقتی $\kappa$ و برچسب‌های رأس $\{x_v\}_{v\in V}$ انتخاب شوند، *تمام
برچسب‌های یالی تعیین می‌شوند*. مسئله وجود EMTL را بنابراین می‌توان به‌صورت
زیر دید:

پیدا کردن $\kappa$ و یک تناظر دوسویی $x:V\to\{1,\dots,N\}$ به‌گونه‌ای که
مقادیر القایی

</div>

$$
\{\kappa - x_u - x_v : uv\in E\}
$$

<div dir="rtl">

(i) همگی اعداد صحیح در $\{1,\dots,N\}$، (ii) همگی متمایز، و (iii) مجزا از
مجموعه برچسب رأسی باشند.

پیاده‌سازی متغیرهای صریح برای برچسب‌های یال نگه می‌دارد چون محدودیت
AllDifferent و تأیید را ساده می‌کند. با این حال، به‌صورت مفهومی، CP-SAT از
همین ساختار بهره‌برداری می‌کند: بسیاری از برچسب‌های یالی به‌سرعت مجبور
می‌شوند، و AllDifferent سپس به‌شدت انتشار پیدا می‌کند.

### ۵.۱۱. لایه SAT (چگونه CP-SAT «یاد می‌گیرد»)

نام **CP-SAT** نشانگر یک دیدگاه ترکیبی است:

- **بخش CP:** متغیرهای صحیح با دامنه‌ها + منتشرکننده‌ها (بخش‌های ۵.۶–۵.۷).
- **بخش SAT:** موتور جستجوی بولی که تصمیمات را ثبت می‌کند، تعارض‌ها را
  تشخیص می‌دهد، و بندها را یاد می‌گیرد.

در داخل، CP-SAT درباره *واقعیت‌های* اتمی به‌عنوان لفظ‌های بولی استدلال
می‌کند مانند:

- "$x \le c$" (یک واقعیت کران بالا)،
- "$x \ge c$" (یک واقعیت کران پایین)،
- گاهی "$x=a$" (که معادل $x\le a$ و $x\ge a$ است).

کوچک شدن دامنه سپس فرایند *تنظیم* برخی از این لفظ‌ها به درست و برخی دیگر
به غلط است. برای مثال، ثابت کردن $x=a$ را می‌توان به‌صورت اثبات موارد زیر
دید:

</div>

$$
x \le a \quad\text{and}\quad x \ge a,
$$

<div dir="rtl">

که هر مقدار دیگری را از دامنه حذف می‌کند.

وقتی CP-SAT انشعاب می‌کند، در واقع یک چنین لفظی را تصمیم می‌گیرد (مثلاً
$x\le c$) و سپس انتشار را می‌گذارد نتایج را استخراج کند. به همین دلیل است
که جستجو را می‌توان به‌صورت یک درخت SAT-مانند از تصمیمات بولی توصیف کرد،
حتی اگر «معنای» هر تصمیم درباره کرانه‌های صحیح باشد.

### ۵.۱۲. از انتشار به nogoodهای آموخته‌شده

یادگیری نیاز دارد که انتشارها با دلایل همراه باشند. به‌صورت مفهومی:

- منتشرکننده‌ها واقعیت‌های کران/مقدار جدید را از موجودی‌ها استخراج می‌کنند.
- اگر یک تناقض ایجاد شود، CP-SAT یک **nogood** استخراج می‌کند: یک بند بولی
  که ترکیب واقعیت‌هایی که تناقض را ایجاد کردند را منع می‌کند.

در EMTL، یک الگوی بسیار رایج این است:

۱. برخی تصمیمات $\kappa$، $x_u$، و $x_v$ را ثابت می‌کنند.
۲. معادله یالی $x_{uv}=\kappa-x_u-x_v$ را به یک مقدار خاص $a$ مجبور
   می‌کند.
۳. AllDifferent (یا دامنه $1\le x_{uv}\le N$) آن مقدار مجبورشده را غیرممکن
   می‌کند.

در سطح بالا، CP-SAT چیزی از این شکل یاد می‌گیرد:

</div>

$$
\neg(\kappa=\kappa_0 \wedge x_u=p \wedge x_v=q) \quad\Longleftrightarrow\quad
(\kappa\ne \kappa_0)\ \vee\ (x_u\ne p)\ \vee\ (x_v\ne q),
$$

<div dir="rtl">

به معنای: «آن ترکیب از تخصیص‌ها را دیگر امتحان نکن.»

اثر عملی این است که CP-SAT صرفاً *عقب‌نشینی* نمی‌کند؛ محدودیت‌هایی جمع
می‌کند که به‌تدریج مناطق بزرگی از فضای جستجو را از بین می‌برند.

---

## ۶. نگاشت پیاده‌سازی (ریاضی → کد)

پیاده‌سازی مرجع در **Python** است و حول `emtl_solver.py` می‌چرخد.

### ۶.۱. ساخت گراف

- **شیء ریاضی:** گراف $G(m,n,k,t)$ و افرازهایش $(A,B,C,D)$.
- **کد:** `GraphParameters` پارامترهای $(m,n,k,t)$ را اعتبارسنجی می‌کند و
  ارائه می‌دهد:
  - `num_vertices = m + 2n + k`،
  - `num_edges = mn + nk + nt`،
  - `total_labels = num_vertices + num_edges`.
- **کد:** `GraphConstructor.construct(params)` یک `networkx.Graph` با رأس‌هایی
  به نام `A0..A(m-1)`، `B0..B(n-1)`، `C0..C(n-1)`، `D0..D(k-1)` ایجاد
  می‌کند. این کد یال‌های زیر را اضافه می‌کند:
  - تمام یال‌ها بین `A` و `B`،
  - تمام یال‌ها بین `C` و `D`،
  - یال‌های میانی از طریق `create_t_regular_bipartite_edges(n,t)` که قانون
    سیرکولانت از بخش ۳.۳ را پیاده‌سازی می‌کند.

### ۶.۲. تولید و حل مدل CP-SAT

- **شیء ریاضی:** متغیرهای $x_v$، $x_e$، و $\kappa$، با محدودیت‌های
  (C1)–(C2).
- **کد:** `EMTLSolver.solve(G)` یک `ortools.sat.python.cp_model.CpModel`
  به‌صورت زیر می‌سازد:
  ۱. `NewIntVar(1, N, ...)` برای هر برچسب رأس و یال،
  ۲. `NewIntVar(6, 3N-3, ...)` برای ثابت جادویی،
  ۳. `AddAllDifferent([...])`,
  ۴. برای هر یال `(u,v)`، اضافه کردن `x_u + x_(u,v) + x_v == kappa`،
  ۵. اجرای `CpSolver().Solve(model)` با محدودیت زمانی و (اختیاری) چندین
     کارگر جستجو.

حل‌کننده یکی از موارد زیر را برمی‌گرداند:
`FOUND` (یک برچسب‌گذاری پیدا شد)، `INFEASIBLE` (هیچ برچسب‌گذاری وجود ندارد،
اثبات‌شده)، یا `TIMEOUT` (در محدوده زمانی به نتیجه‌ای نرسید).

### ۶.۳. تأیید

با داشتن یک راه‌حل ادعایی $(\kappa, f)$، تأیید خطی در $|E|$ است:

۱. بررسی تناظر دوسویی: برچسب‌های استفاده‌شده توسط رأس‌ها و یال‌ها دقیقاً
   $\{1,\dots,N\}$ هستند.
۲. بررسی جادویی: برای هر یال $uv$، بررسی کنید که $f(u)+f(uv)+f(v)=\kappa$.

**کد:** `EMTLSolver.verify_labeling(G, magic_constant, vertex_labels, edge_labels)`
این بررسی‌ها را با اثبات‌های صریح پیاده‌سازی می‌کند.

### ۶.۴. تجسم و رابط کاربری

پروژه شامل موارد زیر است:

- **تجسم Matplotlib:** `EMTLVisualizer.visualize(result, ...)` یک چیدمان
  چهارستونه ثابت (A–B–C–D) رسم می‌کند، افرازها را رنگ‌آمیزی می‌کند، لایه‌های
  یالی را رنگ‌آمیزی می‌کند، و رأس‌ها/یال‌ها را با برچسب‌های به‌دست‌آمده
  حاشیه‌نویسی می‌کند.
- **رابط کاربری Streamlit:** `web/app.py` یک رابط تعاملی برای انتخاب
  $(m,n,k,t)$، تنظیم محدودیت زمانی، اجرای حل‌کننده، و مشاهده نتایج ارائه
  می‌دهد.

---

## ۷. مثال عددی: $G(2,2,2,1)$

پارامترهای $(m,n,k,t)=(2,2,2,1)$ را در نظر بگیرید. آنگاه:

</div>

$$
|V| = 2 + 2\cdot 2 + 2 = 8,\qquad
|E| = (2)(2) + (2)(2) + (2)(1) = 10,\qquad
N=18.
$$

<div dir="rtl">

پیاده‌سازی (یک اجرا) یک EMTL با ثابت جادویی $\kappa=27$ پیدا می‌کند.
یک برچسب‌گذاری معتبر (نه لزوماً یکتا) عبارت است از:

- برچسب‌های رأس:
  - $f(A_0)=11,\; f(A_1)=7$
  - $f(B_0)=15,\; f(B_1)=4$
  - $f(C_0)=10,\; f(C_1)=6$
  - $f(D_0)=8,\; f(D_1)=3$
- برچسب‌های یال (انتخاب‌شده):
  - $f(A_0B_0)=1$ چون $11+1+15=27$
  - $f(B_0C_0)=2$ چون $15+2+10=27$
  - $f(C_0D_0)=9$ چون $10+9+8=27$

در عمل، حل‌کننده دیکشنری‌های کامل برای همه برچسب‌های رأس و یال را
برمی‌گرداند، و `verify_labeling` هر مجموع یالی را بررسی می‌کند.

---

## ۸. نکات عملی برای آزمایش

### ۸.۱. محدودیت‌های زمانی و مقیاس‌پذیری

چون تعداد متغیرهای برچسب $N=|V|+|E|$ است و $|E|$ دارای جملات درجه دوم است
(مثلاً $mn$ و $nk$)، زمان حل می‌تواند با $m,n,k$ به‌سرعت رشد کند. حل‌کننده
مخزن از یک محدودیت زمانی انتخابی کاربر و چندین کارگر CP-SAT پشتیبانی
می‌کند.

### ۸.۲. اجراهای قابل بازتولید

نقطه ورود اصلی `emtl_solver.py` یک مجموعه نمایشی کوچک اجرا می‌کند و
شکل‌ها را در `images/output/` ذخیره می‌کند. برای استفاده برنامه‌ای، فراخوانی
کنید:

</div>

```python
from emtl_solver import solve_emtl

result = solve_emtl(m=2, n=2, k=2, t=1, timeout=60, visualize=False, verbose=False)
if result.exists:
    print(result.magic_constant)
```

<div dir="rtl">

---

## ۹. نتیجه‌گیری

این پروژه برچسب‌گذاری‌های جادویی کل یال را روی یک خانواده گراف چهاربخشی
ساختاریافته $G(m,n,k,t)$ مطالعه می‌کند و مسئله امکان‌سنجی مرتبط را با
استفاده از یک فرمول‌بندی CP-SAT حل می‌کند. مدل ریاضی فشرده است (یک تناظر
دوسویی به‌علاوه یک محدودیت خطی به ازای هر یال)، اما جستجوی ترکیباتی
نتیجه‌شده می‌تواند بزرگ باشد؛ انتشار CP-SAT و یادگیری تعارض‌محور یک روش
دقیق عملی برای بررسی وجود EMTL در رژیم‌های پارامتری مختلف ارائه می‌دهند.

---

## ۱۰. منابع

منابع زیر به‌عنوان مطالعه پس‌زمینه‌ای گنجانده شده‌اند. آن‌ها مستقیماً در
متن اصلی ذکر نشده‌اند؛ برای آشنایی با مفاهیم EMTL و مشاهده مثال‌های
حل‌شده و مستندات استاندارد حل‌کننده استفاده شدند.

۱. *Edge Magic Total Labeling of (n, t)-Kites*، **Advances in Computer Science
   Research**، مجموعه مقالات کنفرانس بین‌المللی ریاضیات، هندسه، آمار، و
   محاسبات (IC-MaGeStiC 2021). موجود در:
   [Atlantis Press](https://www.atlantis-press.com/proceedings/ic-magestic-21/125970045).
۲. *Edge-magic total labelings*. موجود در:
   [Australasian Journal of Combinatorics PDF](https://ajc.maths.uq.edu.au/pdf/22/ocr-ajc-v22-p177.pdf).
۳. *A case study of an edge-magic total labeling of (a,b)-cycle books*.
   موجود در:
   [IOP JPCS PDF](https://iopscience.iop.org/article/10.1088/1742-6596/1940/1/012008/pdf).
۴. *Edge-magic total labeling of some graphs*. موجود در:
   [SSRG ICRMIT PDF](https://www.internationaljournalssrg.org/uploads/specialissuepdf/ICRMIT/2018/MTT/ICRMIT-P116.pdf).
۵. مستندات گوگل OR-Tools، *CP-SAT Solver*. موجود در:
   [developers.google.com](https://developers.google.com/optimization/cp/cp_solver).

</div>
